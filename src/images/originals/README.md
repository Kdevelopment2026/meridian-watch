# Originals

The photography exactly as supplied, at full resolution. Nothing here is
imported by the site.

The files one level up are the versions the site actually ships: the same
frames, resized and recompressed, because a static export serves images as
authored rather than optimising them on request.
